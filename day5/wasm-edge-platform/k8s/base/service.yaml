apiVersion: v1
kind: Service
metadata:
  name: wasm-edge-api-service
  namespace: wasm-edge-platform
spec:
  selector:
    app: wasm-edge-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP
