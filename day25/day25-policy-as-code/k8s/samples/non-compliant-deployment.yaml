apiVersion: apps/v1
kind: Deployment
metadata:
  name: non-compliant-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        # Missing: resources.limits
        # Missing: team and environment labels
